<?xml version="1.0" encoding="UTF-8"?>
<Flow xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>67.0</apiVersion>
    <areMetricsLoggedToDataCloud>false</areMetricsLoggedToDataCloud>
    <assignments>
        <name>Clear_Remarks_On_Decision</name>
        <label>Clear Remarks On Decision</label>
        <locationX>176</locationX>
        <locationY>168</locationY>
        <assignmentItems>
            <assignToReference>$Record.RES_Approval_Request_Remarks__c</assignToReference>
            <operator>Assign</operator>
            <value>
                <elementReference>fEmptyString</elementReference>
            </value>
        </assignmentItems>
    </assignments>
    <description>DIR-869: DEACTIVATED 2026-08-27 per architect direction - the submitter's comment in RES_Approval_Request_Remarks__c must be preserved unmodified for the life of the ApprovalWorkItem, not cleared on decision. Originally cleared Remarks back to blank whenever Status became Approved/Rejected, on the theory that Remarks was request-context only and the approver's own decision remark belongs solely in the native, automation-untouched Comments field. Kept for reference only - do not reactivate without re-confirming with the architect.</description>
    <environments>Default</environments>
    <formulas>
        <name>fEmptyString</name>
        <dataType>String</dataType>
        <expression>&quot;&quot;</expression>
    </formulas>
    <interviewLabel>Approval Work Item - Before Update - Clear Remarks On Decision {!$Flow.CurrentDateTime}</interviewLabel>
    <label>Approval Work Item - Before Update - Clear Remarks On Decision</label>
    <processType>AutoLaunchedFlow</processType>
    <start>
        <locationX>50</locationX>
        <locationY>0</locationY>
        <connector>
            <targetReference>Clear_Remarks_On_Decision</targetReference>
        </connector>
        <filterFormula>AND(
    ISCHANGED({!$Record.Status}),
    OR(
        ISPICKVAL({!$Record.Status}, &quot;Approved&quot;),
        ISPICKVAL({!$Record.Status}, &quot;Rejected&quot;)
    )
)</filterFormula>
        <object>ApprovalWorkItem</object>
        <recordTriggerType>Update</recordTriggerType>
        <triggerType>RecordBeforeSave</triggerType>
    </start>
    <status>Draft</status>
</Flow>
