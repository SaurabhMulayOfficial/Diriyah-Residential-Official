<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RES_Quote_Locked_During_Approval</fullName>
    <active>true</active>
    <description>DIR-763: Quote is locked (no field edits) while discount approval is Negotiating/Pending Approval, except the DOA automation&apos;s own Status transitions (bypass flag checked before and after save).</description>
    <errorConditionFormula>AND(
    NOT($Setup.RES_Config_Switch__c.RES_Validation_Rules_Off__c),
    NOT(RES_System_Status_Update__c),
    NOT(PRIORVALUE(RES_System_Status_Update__c)),
    OR(
        ISPICKVAL(PRIORVALUE(Status), &quot;Pending Approval&quot;)
    )
)</errorConditionFormula>
    <errorDisplayField>Status</errorDisplayField>
    <errorMessage>This Quote is locked while the discount approval is in progress (Pending Approval). Wait for the approval decision before editing.</errorMessage>
</ValidationRule>
