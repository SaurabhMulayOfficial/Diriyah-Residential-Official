<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RES_Expired_Quote_Locked</fullName>
    <active>true</active>
    <description>DIR-763: An Expired Quote is locked (read-only). Blocks any edit when prior Status was Expired, unless system flag set. Bypass checks current+prior flag so the auto-expire job&apos;s set-then-reset toggle isn&apos;t self-blocked.</description>
    <errorConditionFormula>AND(
    NOT($Setup.RES_Config_Switch__c.RES_Validation_Rules_Off__c),
    ISPICKVAL(PRIORVALUE(Status), &quot;Expired&quot;),
    NOT(RES_System_Status_Update__c),
    NOT(PRIORVALUE(RES_System_Status_Update__c))
)</errorConditionFormula>
    <errorDisplayField>Status</errorDisplayField>
    <errorMessage>This quote has expired and can no longer be used or edited.</errorMessage>
</ValidationRule>
