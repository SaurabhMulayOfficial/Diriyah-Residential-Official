<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>DIR-1128: Publish-Immediately platform event used to surface a real-time alert when a Quote Line Item add is blocked (e.g., from Browse Catalogs, which swallows save-time error messages). Published from the QLI validation flow before it throws the custom error; because it publishes immediately it survives the transaction rollback.</description>
    <eventType>HighVolume</eventType>
    <label>RES Line Item Alert</label>
    <pluralLabel>RES Line Item Alerts</pluralLabel>
    <publishBehavior>PublishImmediately</publishBehavior>
</CustomObject>
