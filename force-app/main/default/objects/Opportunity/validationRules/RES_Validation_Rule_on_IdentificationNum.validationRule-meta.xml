<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RES_Validation_Rule_on_IdentificationNum</fullName>
    <active>false</active>
    <errorConditionFormula>AND(
    NOT($Setup.RES_Config_Switch__c.RES_Validation_Rules_Off__c),

    OR(
        $RecordType.DeveloperName = &quot;RES_Residential_Single_Buyer&quot;,
        $RecordType.DeveloperName = &quot;RES_Residential_Multi_Buyer&quot;
    ),

    NOT(ISPICKVAL(StageName, &quot;Closed Lost&quot;)),

    OR(
        AND(
            ISPICKVAL(RES_Identification_Type__c, &quot;ZIT01&quot;),
            NOT(
                REGEX(RES_Identification_Number__c, &quot;^1[0-9]{9}$&quot;)
            )
        ),

        AND(
            ISPICKVAL(RES_Identification_Type__c, &quot;ZIT02&quot;),
            NOT(
                REGEX(RES_Identification_Number__c, &quot;^2[0-9]{9}$&quot;)
            )
        ),

        AND(
            ISPICKVAL(RES_Identification_Type__c, &quot;ZIT03&quot;),
            NOT(
                REGEX(RES_Identification_Number__c , &quot;^[a-zA-Z0-9]{1,20}$&quot;)
            )
        )
    )
)</errorConditionFormula>
    <errorDisplayField>RES_Identification_Number__c</errorDisplayField>
    <errorMessage>Invalid Identification Number. National ID must start with 1 and be up to 10 digits. Iqama must start with 2 and be up to 10 digits. Passport must be alphanumeric and can contain max 20 characters.</errorMessage>
</ValidationRule>
