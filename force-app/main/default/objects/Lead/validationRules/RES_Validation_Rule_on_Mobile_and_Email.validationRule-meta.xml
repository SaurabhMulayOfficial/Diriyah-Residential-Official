<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RES_Validation_Rule_on_Mobile_and_Email</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    NOT($Setup.RES_Config_Switch__c.RES_Validation_Rules_Off__c),

    /* Apply only for required Record Types */
    OR(
        $RecordType.DeveloperName = &quot;RES_Company&quot;,
        $RecordType.DeveloperName = &quot;RES_Individual&quot;
    ),

    OR(

        /* 🔴 Case 1: Wadi Safar RYI → Email Mandatory */
        AND(
            ISPICKVAL(RES_Interest_Type__c , &quot;WADISAFARRYI&quot;),
            ISBLANK(Email)
        ),

        /* 🔴 Case 2: Other Interest → Mobile Mandatory */
        AND(
            NOT(ISPICKVAL(RES_Interest_Type__c , &quot;WADISAFARRYI&quot;)),
            ISBLANK(MobilePhone)
        )
    )
)</errorConditionFormula>
    <errorMessage>For Interest Type = &quot;Wadi Safar RYI&quot;, email is mandatory.
For all other interest types or when no selection is made, the mobile number is mandatory.</errorMessage>
</ValidationRule>
